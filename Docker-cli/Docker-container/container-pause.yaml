✅ Docker container pause

docker container pause suspends all processes in a container by sending them the SIGSTOP signal. 

This essentially freezes the container in its current state, allowing you to resume later using docker container unpause.

⚠️ While a container is paused, it consumes minimal CPU, but memory remains allocated.

---
🔹 1. Basic Syntax

docker container pause CONTAINER [CONTAINER...]

Shortcut:

docker pause <container_name_or_id>

docker pause is an alias for docker container pause.

---
🔹 2. How It Works

Sends SIGSTOP to all processes in the container

Paused processes cannot execute, but remain in memory

Container remains visible in docker ps, with a paused status

Resume container using:

docker container unpause <container_name>

---
🔹 3. Examples

Example 1 — Pause a single container

docker container pause mynginx

Output:

mynginx

Container processes are frozen, no CPU usage, memory intact

--
Example 2 — Pause multiple containers

docker container pause mynginx myubuntu

Output:

mynginx
myubuntu

Useful in scripts or batch operations

--
Example 3 — Unpause a container

docker container unpause mynginx

Output:

mynginx

Container resumes from exact state before pause

--
Example 4 — Check paused containers

docker container ls

Output:

CONTAINER ID   IMAGE     STATUS
c1b2d3e4f5g6   nginx     Up 2 hours (Paused)

---
🔹 4. Use Cases

Temporarily free CPU for host without stopping containers

Example: Pause heavy container during peak load

Debugging or snapshots

Pause a container to inspect memory or filesystem

CI/CD pipelines

Pause dependent containers temporarily during tests

Testing process state management

Observe container behavior when frozen

---
🔹 5. Differences from Stop and Kill

| Command          | Signal            | Graceful Shutdown  | CPU Usage         | Memory Usage    | Use Case                |
| ---------------- | ----------------- | ------------------ | ----------------- | --------------- | ----------------------- |
| `docker stop`    | SIGTERM → SIGKILL | ✅                  | 0                 | 0               | Graceful termination    |
| `docker kill`    | SIGKILL           | ❌                  | 0                 | 0               | Immediate stop          |
| `docker pause`   | SIGSTOP           | ❌ (process frozen) | 0                 | Memory retained | Temporary suspension    |
| `docker unpause` | SIGCONT           | ❌                  | Resumes CPU usage | Memory intact   | Resume paused container |

---
🔹 6. Best Practices

Use pause only when temporary suspension is needed

Avoid pausing containers holding critical I/O operations unless safe

Monitor paused containers with docker container ls

Combine with resource management scripts to control CPU load

---
🔹 7. Quick Reference Table

| Task                      | Command                                   | Notes                            |
| ------------------------- | ----------------------------------------- | -------------------------------- |
| Pause a container         | `docker container pause mynginx`          | Suspends all processes (SIGSTOP) |
| Pause multiple containers | `docker container pause mynginx myubuntu` | Batch pause                      |
| Unpause a container       | `docker container unpause mynginx`        | Resumes container (SIGCONT)      |
| Check paused status       | `docker container ls`                     | Status shows “Paused”            |

---
✅ Summary:

docker container pause = freeze container processes temporarily

Uses SIGSTOP, CPU usage drops to zero, memory remains allocated

Resume using docker container unpause

Useful for resource management, debugging, and temporary suspension